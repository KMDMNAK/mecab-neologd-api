version: "3"
services: 
    mecab-api:
        container_name: mecabapi
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "3000:3000"
        environment: 
            PORT: 3000